[package]
name = "guest-control-proto"
version = "0.21.12"
edition.workspace = true
description = "Vsock binary protocol for host-guest communication"

[dependencies]
guest-contracts.workspace = true

[dev-dependencies]
proptest = { workspace = true, features = ["std"] }

[lints]
workspace = true
