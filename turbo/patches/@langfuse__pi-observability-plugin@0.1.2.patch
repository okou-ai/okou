diff --git a/package.json b/package.json
index d41363e0c536ed26a6e8e761b4bbbef41988001f..cf23501694339325acc46f17f4119407323485d7 100644
--- a/package.json
+++ b/package.json
@@ -5,6 +5,7 @@
   "license": "MIT",
   "author": "Langfuse",
   "type": "module",
+  "exports": "./src/index.ts",
   "packageManager": "pnpm@11.22.0",
   "repository": {
     "type": "git",
diff --git a/src/index.ts b/src/index.ts
index 363a50de0d9702d7c365efcd6ade09e534713261..5cff41846d8796488029140ebca18836ec757b26 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -14,11 +14,14 @@ import {
 } from "@langfuse/tracing";
 import { type SpanContext, TraceFlags } from "@opentelemetry/api";
 import { AlwaysOnSampler, NodeTracerProvider } from "@opentelemetry/sdk-trace-node";
+import { OTLPTraceExporter } from "@opentelemetry/exporter-trace-otlp-http";
 
 const EXTENSION_NAME = "@langfuse/pi-observability-plugin";
 const EXTENSION_VERSION = "0.1.2";
 const ROOT_OBSERVATION_NAME = "Conversational Turn";
 const SUBAGENT_ROOT_OBSERVATION_NAME = "Subagent Turn";
+const CONTINUATION_ROOT_OBSERVATION_NAME = "Sandbox Continuation";
+const SANDBOX_ROOT_OBSERVATION_NAME = "Sandbox Execution";
 const TRACE_NAME = "Pi Turn";
 const GENERATION_PREFIX = "LLM Call";
 const TOOL_PREFIX = "Tool:";
@@ -76,9 +79,10 @@ export function readInheritedParent(env: NodeJS.ProcessEnv = process.env): Inher
 // ---------------------------------------------------------------------------
 
 export interface LangfuseConfig {
-  publicKey: string;
-  secretKey: string;
-  baseUrl: string;
+  publicKey?: string;
+  secretKey?: string;
+  baseUrl?: string;
+  relay?: { endpoint: string; token: string };
   userId?: string;
   environment?: string;
   release?: string;
@@ -89,6 +93,19 @@ export function loadConfig(): LangfuseConfig | undefined {
   if ((process.env.LANGFUSE_TRACING_ENABLED ?? "").trim().toLowerCase() === "false") {
     return undefined;
   }
+  // Platform telemetry uses run authentication against the first-party API.
+  // Never fall back to a user Langfuse config when relay configuration is partial.
+  const endpoint = process.env.OKOU_PI_LANGFUSE_OTLP_ENDPOINT?.trim();
+  const token = process.env.OKOU_PI_LANGFUSE_OTLP_TOKEN?.trim();
+  if (endpoint || token) {
+    if (!endpoint || !token) return undefined;
+    return {
+      relay: { endpoint, token },
+      userId: process.env.LANGFUSE_USER_ID?.trim(),
+      environment: process.env.LANGFUSE_TRACING_ENVIRONMENT?.trim(),
+      release: process.env.LANGFUSE_RELEASE?.trim(),
+    };
+  }
   const file = readConfigFile();
   const asTrimmedString = (v: unknown): string | undefined =>
     typeof v === "string" && v.trim() ? v.trim() : undefined;
@@ -144,14 +161,15 @@ export interface TruncationMeta {
 }
 
 export function truncateText(text: string): { text: string; meta: TruncationMeta } {
-  if (text.length <= MAX_CHARS) {
-    return { text, meta: { truncated: false, orig_len: text.length } };
+  const redacted = redactLangfuseKeys(text) as string;
+  if (redacted.length <= MAX_CHARS) {
+    return { text: redacted, meta: { truncated: false, orig_len: redacted.length } };
   }
   return {
-    text: text.slice(0, MAX_CHARS),
+    text: redacted.slice(0, MAX_CHARS),
     meta: {
       truncated: true,
-      orig_len: text.length,
+      orig_len: redacted.length,
       kept_len: MAX_CHARS,
       sha256: createHash("sha256").update(text).digest("hex"),
     },
@@ -197,7 +215,7 @@ export function createSecretRedactor(...extraSecrets: string[]): (value: unknown
   return (value) => walk(value, []);
 }
 
-const redactLangfuseKeys = createSecretRedactor();
+const redactLangfuseKeys = createSecretRedactor(process.env.OKOU_PI_LANGFUSE_OTLP_TOKEN ?? "");
 
 /** Extract plain text from a pi message content array. */
 export function extractText(content: unknown): string {
@@ -383,8 +401,18 @@ function createRuntime(
   config: LangfuseConfig,
   resolveTraceAttributes: () => TraceAttributes,
 ): Runtime {
-  const redactSecrets = createSecretRedactor(config.publicKey, config.secretKey);
+  const redactSecrets = createSecretRedactor(config.publicKey ?? "", config.secretKey ?? "", config.relay?.token ?? "");
   const processor = new LangfuseSpanProcessor({
+    ...(config.relay ? {
+      exporter: new OTLPTraceExporter({
+        url: config.relay.endpoint,
+        headers: { Authorization: `Bearer ${config.relay.token}` },
+        timeoutMillis: 10_000,
+      }),
+      // Keep request batches within the first-party API's payload bound.
+      flushAt: 16,
+      mediaUploadEnabled: false,
+    } : {}),
     publicKey: config.publicKey,
     secretKey: config.secretKey,
     baseUrl: config.baseUrl,
@@ -494,6 +522,36 @@ export default function (pi: ExtensionAPI) {
     }
   };
 
+  const isPlatformSandbox = Boolean(config.relay && inheritedParent?.depth === 0);
+  const sandboxWaitStartedAt = Number(process.env.OKOU_PI_LANGFUSE_SANDBOX_WAIT_STARTED_AT);
+  let sandboxWaitRecorded = false;
+  const recordSandboxWait = (endedAt: Date) => {
+    if (sandboxWaitRecorded || !isPlatformSandbox || !inheritedParent) return;
+    sandboxWaitRecorded = true;
+    // Different hosts supply these timestamps; do not fabricate a duration on clock skew.
+    if (!Number.isSafeInteger(sandboxWaitStartedAt) || sandboxWaitStartedAt < 0 || sandboxWaitStartedAt > endedAt.getTime()) return;
+    const wait = startObservation(
+      "Sandbox Wait",
+      {
+        metadata: {
+          source: "pi",
+          extension: EXTENSION_NAME,
+          extension_version: EXTENSION_VERSION,
+          phase: "sandbox-wait",
+          start_boundary: "manifest-publication-start",
+          end_boundary: "sandbox-execution-start",
+          content_capture: "metadata-only",
+        },
+      },
+      {
+        asType: "span",
+        parentSpanContext: inheritedParent.spanContext,
+        startTime: new Date(sandboxWaitStartedAt),
+      },
+    );
+    wait.end(endedAt);
+  };
+
   const closeDanglingObservations = (reason: "interrupted" | "superseded") => {
     if (!state) return;
     for (const [, tool] of state.openTools) {
@@ -618,8 +676,10 @@ export default function (pi: ExtensionAPI) {
       ...(config.userId ? { [LangfuseOtelSpanAttributes.TRACE_USER_ID]: config.userId } : {}),
     };
 
+    const startedAt = new Date();
+    recordSandboxWait(startedAt);
     const root = startObservation(
-      isSubagent ? SUBAGENT_ROOT_OBSERVATION_NAME : ROOT_OBSERVATION_NAME,
+      isPlatformSandbox ? SANDBOX_ROOT_OBSERVATION_NAME : isSubagent ? SUBAGENT_ROOT_OBSERVATION_NAME : ROOT_OBSERVATION_NAME,
       {
         input: { role: "user", content: userText },
         metadata: {
@@ -637,7 +697,7 @@ export default function (pi: ExtensionAPI) {
             : {}),
         },
       },
-      { asType: "span", ...(inheritedParent ? { parentSpanContext: inheritedParent.spanContext } : {}) },
+      { asType: "span", startTime: startedAt, ...(inheritedParent ? { parentSpanContext: inheritedParent.spanContext } : {}) },
     );
 
     state = {
@@ -654,6 +714,88 @@ export default function (pi: ExtensionAPI) {
     debug("root created, turn", turnNumber);
   });
 
+  // continuePendingTools() starts the agent without before_agent_start because
+  // the user prompt and assistant tool calls already exist in session history.
+  pi.on("agent_start", (_event, ctx) => {
+    if (
+      state ||
+      !inheritedParent ||
+      process.env.PI_LANGFUSE_CONTINUATION !== "true"
+    ) {
+      return;
+    }
+
+    ensureRuntime();
+    const sessionId = ctx.sessionManager.getSessionId();
+    const entries = ctx.sessionManager.getEntries() as Array<{
+      type?: string;
+      message?: { role?: string; content?: unknown };
+    }>;
+    let lastUserMessage: (typeof entries)[number] | undefined;
+    for (let index = entries.length - 1; index >= 0; index -= 1) {
+      const entry = entries[index];
+      if (entry?.type === "message" && entry.message?.role === "user") {
+        lastUserMessage = entry;
+        break;
+      }
+    }
+    const { text: userText, meta: userMeta } = truncateText(
+      extractText(lastUserMessage?.message?.content),
+    );
+    const turnNumber = resolveTurnNumber(ctx, userText);
+    lastPromptText = userText;
+    traceAttributes = {
+      [LangfuseOtelSpanAttributes.TRACE_SESSION_ID]:
+        inheritedParent.sessionId ?? sessionId,
+      [LangfuseOtelSpanAttributes.TRACE_TAGS]: BASE_TAGS,
+      ...(config.userId
+        ? { [LangfuseOtelSpanAttributes.TRACE_USER_ID]: config.userId }
+        : {}),
+    };
+
+    const startedAt = new Date();
+    recordSandboxWait(startedAt);
+    const root = startObservation(
+      isPlatformSandbox ? SANDBOX_ROOT_OBSERVATION_NAME : CONTINUATION_ROOT_OBSERVATION_NAME,
+      {
+        input: { role: "user", content: userText },
+        metadata: {
+          source: "pi",
+          extension: EXTENSION_NAME,
+          extension_version: EXTENSION_VERSION,
+          session_id: sessionId,
+          turn_number: turnNumber,
+          cwd: ctx.cwd,
+          user_text_meta: userMeta,
+          continuation: "pending-tools",
+          parent_session_id: inheritedParent.sessionId,
+          ...(gitBranch ? { git_branch: gitBranch } : {}),
+          ...(ctx.model
+            ? { model: ctx.model.id, provider: ctx.model.provider }
+            : {}),
+        },
+      },
+      {
+        asType: "span",
+        parentSpanContext: inheritedParent.spanContext,
+        startTime: startedAt,
+      },
+    );
+
+    state = {
+      root,
+      turnNumber,
+      generationCount: 0,
+      openTools: new Map(),
+      pendingToolResults: [],
+      sawError: false,
+      userText,
+      turnImages: [],
+    };
+    publishParentContext(root, sessionId);
+    debug("continuation root created, turn", turnNumber);
+  });
+
   pi.on("before_provider_request", (_event, ctx) => {
     if (!state) return;
     // A new provider request while one is open means the previous HTTP
