import { PRESIGNED_URL_TTL_SECONDS } from "@okouai/api-contracts/contracts/presigned-urls";

/** Read credentials for private files and isolated HTML previews. */
export const PRIVATE_ARTIFACT_PREVIEW_TTL_SECONDS = PRESIGNED_URL_TTL_SECONDS;
